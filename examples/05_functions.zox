// Functions, recursion, early return
fun add(a, b) {
  return a + b;
}
print add(3, 4);          // 7

fun factorial(n) {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
}
print factorial(5);       // 120

fun early() {
  return 1;
  print "unreachable";
}
print early();            // 1
